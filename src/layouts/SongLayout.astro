---
import type { CollectionEntry } from "astro:content";
import Layout from "./Layout.astro";

import { songsCollection } from "../content/collections";
import ProviderWrapper from "../components/ProviderWrapper.vue";

const songs = songsCollection;

interface Props {
  song: CollectionEntry<"songs_ru">;
}

const { song } = Astro.props;

const { title } = song.data;
---

<Layout title={title}>
  <ProviderWrapper song={song} songs={songs} client:only />
</Layout>
